export interface IFavorite {
  discount: number;
  imageUrl: string;
  isActive: boolean;
  name: string;
  originalPrice: number;
  price: number;
  productId: number;
  stock: number;
  category: string;
}
